import InventoryPagePlaceholder from "./InventoryPagePlaceholder";

const StockTransfer = () => (
  <InventoryPagePlaceholder
    title="Stock Transfer"
    description="Move stock between locations (e.g. main store → kitchen, bar → restaurant) and log the transfer."
  />
);

export default StockTransfer;
