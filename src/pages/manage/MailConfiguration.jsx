import ManagePagePlaceholder from "./ManagePagePlaceholder";

const MailConfiguration = () => (
  <ManagePagePlaceholder
    title="Mail Configuration"
    description="SMTP settings for sending invoices, receipts, and notifications."
  />
);

export default MailConfiguration;
