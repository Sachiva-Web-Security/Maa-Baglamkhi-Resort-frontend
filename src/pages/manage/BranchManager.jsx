import ManagePagePlaceholder from "./ManagePagePlaceholder";

const BranchManager = () => (
  <ManagePagePlaceholder
    title="Branch Manager"
    description="Add and configure branches/properties under the group."
  />
);

export default BranchManager;
