import FBPagePlaceholder from "./FBPagePlaceholder";

const Tables = () => (
  <FBPagePlaceholder
    title="Manage Tables"
    description="Add and configure individual restaurant tables (number, capacity, group, status)."
  />
);

export default Tables;
